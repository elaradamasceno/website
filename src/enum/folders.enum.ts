export enum Summary {
  fileType = 'md',
  page = '/summary'
}

export enum SocialMedia {
  fileType = 'md',
  page = '/social-media'
}

export enum GitHub {
  fileType = 'txt',
  page = '/github'
}

export enum Curriculum {
  fileType = 'code',
  page = '/curriculum'
}

export enum Home {
  folderTitle ='Home',
  page = '/'
}