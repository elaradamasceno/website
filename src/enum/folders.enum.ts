export enum Summary {
  folderTitle = 'Resumo Profissional',
  fileType = 'md',
  page = '/summary'
}

export enum SocialMedia {
  folderTitle = 'Mídias Sociais',
  fileType = 'md',
  page = '/social-media'
}

export enum GitHub {
  folderTitle = 'GitHub',
  fileType = 'txt',
  page = '/github'
}

export enum Curriculum {
  folderTitle = 'Currículo',
  fileType = 'code',
  page = '/curriculum'
}

export enum Home {
  folderTitle ='Home',
  page = '/'
}